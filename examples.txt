def suma = lambda.y:Nat. y + 1 ;;

eval suma 3;;

x:Nat = 3 ;;

pred(succ(x)) + succ(0);;

isZero(succ(1));;

def factorial = lambda.n:Nat. if isZero(n) then 1 else (n * factorial(pred(n))) ;;

eval factorial 5;;

if succ(0) then if isZero(0) then r:Nat = 1 else 1 else 3;;

